# Identity
user.name: yourname
user.email: your-email@example.com

# Hygiene & Automation
fetch.prune: true
column.ui: auto
branch.sort: -committerdate


# Better Diffing
diff.algorithm: histogram
diff.colorMoved: plain
diff.mnemonicPrefix: true

# Security (Optional, requires GPG key)
# commit.gpgsign: false

